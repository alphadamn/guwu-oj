CATEGORY = {
    'title': '算法教程',
    'icon': 'bi-diagram-3',
    'description': '从入门到提高：基础算法、数据结构、图论、DP、数学等。',
    'articles': {
        'complexity': {
            'title': '复杂度分析',
            'summary': '大 O 记号、常见上限与估时',
            'content': r'''
## 大 O 记号

描述算法随规模 $n$ 增长的上界，忽略常数和低阶项。

| 记号 | 名称 | $n=10^5$ 量级 |
|------|------|----------------|
| $O(1)$ | 常数 | 极快 |
| $O(\log n)$ | 对数 | 极快 |
| $O(n)$ | 线性 | 通常可行 |
| $O(n \log n)$ | 线性对数 | 排序、分治 |
| $O(n^2)$ | 平方 | $n \le 5000$ 左右 |
| $O(2^n)$ | 指数 | $n \le 20\sim 25$ |

## 如何估算

1. 看**最内层循环**执行次数
2. 递归：写出递推式或层数 × 每层工作量
3. 多组数据：注意 $\sum n$ 是否与题目限制一致

## 空间复杂度

数组开多大、递归栈深度、是否用滚动数组优化 DP。

## 卡常

同样 $O(n \log n)$，常数差 10 倍可能决定 TLE。见「优化技巧」一章。

## 例题

**例 1：估复杂度是否可行**  
数据 $n \le 10^5$，双重循环 `for i` `for j` 枚举所有点对 → $O(n^2)$ 约 $10^{10}$ 次，必 TLE；应改为排序 + 双指针 $O(n \log n)$ 或哈希 $O(n)$。

**例 2：多组数据 $\sum n$**  
「$T$ 组，每组一条长度为 $n$ 的序列，$\sum n \le 10^5$」：单组可写 $O(n^2)$，但**所有组合并**不能超过 $10^5$，否则总复杂度爆掉。
''',
        },
        'sorting': {
            'title': '排序与二分',
            'summary': '排序算法、二分答案、lower_bound',
            'content': r'''
## 排序

竞赛中几乎总是用库函数：

- C++：`sort(a, a+n)` 平均 $O(n \log n)$
- 需要稳定排序：`stable_sort`
- 自定义比较：`sort(v.begin(), v.end(), [](const auto& x, const auto& y) { return x.second < y.second; });`

## 离散化

值域很大但个数少时，把值映射到 $1\sim k$：

```cpp
vector<int> all = a;
sort(all.begin(), all.end());
all.erase(unique(all.begin(), all.end()), all.end());
int id = lower_bound(all.begin(), all.end(), x) - all.begin();
```

## 二分答案

当「判定是否可行」比「直接求最优」容易时：

1. 确定答案区间 $[L, R]$
2. `while (L < R)` 取 `mid`，若可行则收缩一侧
3. 注意边界：`mid = (L + R + 1) / 2` 避免死循环

## 二分查找

在**有序数组**上：

- `lower_bound`：第一个 $\ge x$
- `upper_bound`：第一个 $> x$
- 个数：`upper_bound - lower_bound`

## 三分

单峰函数求极值：在 $[L,R]$ 上取三等分点比较，适用于凸函数或明确单峰的性质题。

## 计数排序 / 基数排序

值域小（如 $10^6$ 以内）时可 $O(n)$ 或接近线性，配合离散化使用。

## 例题

**例 1：砍树 / 木材加工（二分答案）**  
有 $n$ 棵树，高度 $h_i$，要得到至少 $m$ 米木材。设统一砍到高度 $H$，可得 $\sum \max(0, h_i - H)$。$H$ 越大木材越少，**二分 $H$**，每次 $O(n)$ 检查是否 $\ge m$。复杂度 $O(n \log \max h)$。

**例 2：逆序对 / 排名（离散化 + 树状数组）**  
值域 $10^9$ 但只有 $n$ 个数，先**排序去重**得到排名 `id`，再在树状数组上从左到右插入，查询比当前值小的个数即为逆序对贡献。体现「排序 + 离散化」组合拳。
''',
        },
        'search': {
            'title': '搜索与 DFS/BFS',
            'summary': '回溯、剪枝、记忆化、广度优先',
            'content': r'''
## 深度优先搜索 DFS

用于：全排列、组合、连通块、树遍历、回溯、强连通分量（Tarjan）等。

```cpp
void dfs(int u) {
    vis[u] = true;
    for (int v : adj[u])
        if (!vis[v]) dfs(v);
}
```

**回溯模板**（排列 / 子集 / N 皇后）：

```cpp
void backtrack(int step) {
    if (step == n) { record_answer(); return; }
    for (int choice : choices_at(step)) {
        if (!valid(choice)) continue;
        apply(choice);
        backtrack(step + 1);
        undo(choice);
    }
}
```

## 剪枝

- **可行性剪枝**：当前状态已不可能得到合法解（如和已超过目标）
- **最优性剪枝**：当前代价已 $\ge$ 已知最优解（求最小值时）
- **排序后搜索**：分支按代价排序，配合最优性剪枝效果更好
- **记忆化**：状态空间可哈希或编号时，避免重复 DFS

## 广度优先搜索 BFS

用于：**无权图最短路**、层次遍历、**状态空间最短路**（如棋盘最少步数）。

```cpp
queue<int> q;
q.push(start); dist[start] = 0;
while (!q.empty()) {
    int u = q.front(); q.pop();
    for (int v : adj[u])
        if (dist[v] == -1) {
            dist[v] = dist[u] + 1;
            q.push(v);
        }
}
```

状态 BFS：将「局面」编码为整数或结构体入队，注意去重（`unordered_set` 或 `vis` 数组）。

## 双向 BFS

起点、终点同时扩展，相遇时层数之和即为答案，状态空间呈指数时效果明显。

## 记忆化搜索

DFS + 数组缓存结果，是自顶向下 DP，适合状态多、转移写递归更直观（数位 DP、树形 DP）。

## 迭代加深 DFS（IDDFS）

在深度限制下反复 DFS，适合**解可能在较深层**但分支因子大的情况，空间 $O(d)$ 优于 BFS 的 $O(分支^d)$。

## 例题

**例 1：迷宫最短路（BFS）**  
$n \times m$ 网格，`.` 可走 `#` 障碍，求 $(1,1)$ 到 $(n,m)$ 最少步数。四方向扩展，`dist` 初值 $-1$，第一次到达即最短。复杂度 $O(nm)$。

**例 2：N 皇后（回溯 + 剪枝）**  
在 $n \times n$ 棋盘放 $n$ 个皇后互不攻击。按行 DFS 选列，用数组记录列、主对角线、副对角线是否占用；**可行性剪枝**跳过冲突列。$n=8$ 时即可秒出，$n \le 12$ 常用。
''',
        },
        'dp': {
            'title': '动态规划 DP',
            'summary': '状态设计、经典模型、转移与优化',
            'content': r'''
## 核心思想

将原问题拆成**重叠子问题**，用表格保存子问题最优解，避免重复计算。

适用需同时满足：

- **最优子结构**：全局最优可由子问题最优组合得到
- **无后效性**：当前状态已包含做决策所需的全部历史信息

若只能证明「局部贪心」正确，用贪心；若子问题会重复出现且可合并，优先考虑 DP。

## 四步模板

1. **定义状态**：`dp[i]`、`dp[i][j]`、`dp[mask]` 等分别表示什么（尽量一维、语义清晰）
2. **初始条件**：`dp[0]`、空集、叶子节点等边界如何填
3. **转移方程**：枚举「最后一步 / 决策」从已知状态推到未知
4. **答案**：`max(dp[n])`、`dp[0][n-1]`、所有终点状态的最值等

写代码前建议先画表格或 DAG，确认转移方向（从小到大 / 区间长度递增）。

## 线性 DP

### 最长上升子序列（LIS）

**$O(n^2)$**：`dp[i]` = 以 `a[i]` 结尾的 LIS 长度。

```cpp
for (int i = 0; i < n; ++i) {
    dp[i] = 1;
    for (int j = 0; j < i; ++j)
        if (a[j] < a[i]) dp[i] = max(dp[i], dp[j] + 1);
}
```

**$O(n \log n)$**：维护长度为 $k$ 的上升子序列的**最小末尾**数组 `tail[]`，对每个 `a[i]` 用 `lower_bound` 替换或追加。

### 最长公共子序列（LCS）

`dp[i][j]` = `s[0..i)` 与 `t[0..j)` 的 LCS 长度：

- 若 `s[i-1] == t[j-1]`：`dp[i][j] = dp[i-1][j-1] + 1`
- 否则：`dp[i][j] = max(dp[i-1][j], dp[i][j-1])`

空间可压成两行滚动数组。

### 编辑距离

`dp[i][j]` = 将 `s[0..i)` 变成 `t[0..j)` 的最少操作数（插入、删除、替换），转移与 LCS 类似，字符不等时取三种操作 +1 的最小值。

### 最大子段和

`dp[i]` = 以 `a[i]` 结尾的最大子段和：`dp[i] = max(a[i], dp[i-1] + a[i])`，全程维护全局最大值。可 $O(1)$ 空间。

## 背包 DP

一维数组 `dp[j]` 表示容量为 $j$ 时的最优值，**内层循环容量方向**决定背包类型。

### 0/1 背包

每个物品最多选一次，**容量从大到小**：

```cpp
for (int i = 0; i < n; ++i)
    for (int j = W; j >= w[i]; --j)
        dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
```

### 完全背包

每个物品无限个，**容量从小到大**：

```cpp
for (int i = 0; i < n; ++i)
    for (int j = w[i]; j <= W; ++j)
        dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
```

### 多重背包

物品 $i$ 最多选 `c[i]` 次：可二进制拆分体积为 $1,2,4,\ldots$ 的 0/1 物品，或用单调队列优化到 $O(nW)$。

### 二维费用

两种限制（体积 + 重量）时：`dp[j][k]`，转移与 0/1 类似，两层容量都逆序。

## 区间 DP

合并、括号匹配、石子合并等：先算小区间，再算大区间。

`dp[l][r]` = 合并区间 $[l,r]$ 的最优代价，枚举分割点 $k$：

```cpp
for (int len = 2; len <= n; ++len)
    for (int l = 0; l + len - 1 < n; ++l) {
        int r = l + len - 1;
        dp[l][r] = INF;
        for (int k = l; k < r; ++k)
            dp[l][r] = min(dp[l][r], dp[l][k] + dp[k+1][r] + cost(l, r));
    }
```

注意：环上区间可先断环复制一倍，或枚举起点。

## 树形 DP

在树上 DFS，自底向上合并子树信息。常见状态：

- `dp[u][0/1]`：选 / 不选节点 $u$（最大独立集、没有上司的舞会）
- `dp[u]`：以 $u$ 为根的子树最优值（换根 DP 需二次 DFS）

```cpp
void dfs(int u, int fa) {
    dp[u] = val[u];
    for (int v : adj[u]) if (v != fa) {
        dfs(v, u);
        dp[u] = max(dp[u], dp[u] + dp[v]);  // 依题意合并
    }
}
```

**换根 DP**：第一次求以 1 为根的答案；第二次 DFS 把根从 $u$ 换到子节点 $v$，用 $O(1)$ 或 $O(\deg)$ 更新父节点贡献。

## 状压 DP

集合用二进制 `mask` 表示，第 $i$ 位为 1 表示已选第 $i$ 个元素。

- **旅行商 TSP**：`dp[mask][i]` 已访问集合 `mask`、当前在 $i$ 的最小代价，$n \le 20$ 左右
- **子集枚举**：`for (int s = mask; s; s = (s-1) & mask)` 枚举 `mask` 的子集，复杂度 $O(3^n)$ 级别需注意

## 数位 DP

统计 $[L,R]$ 内满足数位约束的个数（不含前导零、模意义下等）。按**位**从高到低 DFS，状态通常包括：当前位、是否紧贴上界、是否已开始填数、余数等，用 `memo[pos][tight][started][...]` 记忆化。

关键：区分「前导零」与「真正数字」的转移，边界 $L,R$ 可分别 DP 后相减。

## 滚动数组与空间优化

只依赖上一层时：

```cpp
for (int i = 0; i < n; ++i)
    for (int j = 0; j <= W; ++j)
        dp[i % 2][j] = ...;  // 或两个一维数组 swap
```

区间 DP 若只依赖更短区间，不能简单滚动，需保留二维。

## 进阶优化（了解）

| 技巧 | 典型场景 |
|------|----------|
| 单调队列优化 | 转移形如 $\max_{k<i}(dp[k] + w(k,i))$，$w$ 满足四边形不等式 |
| 斜率优化 | 转移可写成直线求最值，维护凸包 |
| 矩阵快速幂 | 固定长度线性递推求第 $n$ 项，$O(k^3 \log n)$ |
| Bitmask + SOS | 子集和 / 超集和 DP |

## 常见错误

- 循环方向错（0/1 背包正序导致同一物品用多次）
- 状态定义含糊（「前 $i$ 个」是否包含第 $i$ 个）
- 初始化：求最小值时 `INF` 是否够大；空集 / 空串的 `dp` 是否为 0
- 答案取错下标（LIS 要 `max(dp[i])`，不是 `dp[n-1]`）

## 调试建议

$n \le 15$ 时对拍暴力；打印 DP 表检查边界；画图确认 DAG 无环与转移顺序。

## 例题

**例 1：采药 / 0-1 背包（洛谷 P1048）**  
$n$ 种物品，体积 $w_i$、价值 $v_i$，背包容积 $W$，每种最多选一次。`dp[j] = max(dp[j], dp[j-w_i]+v_i)`，$j$ 从 $W$ 到 $w_i$ 逆序。答案 `dp[W]`。复杂度 $O(nW)$。

**例 2：石子合并（区间 DP）**  
一行 $n$ 堆石子，每次合并相邻两堆，代价为两堆之和，求合并成一堆的最小代价。`dp[l][r]` 为合并 $[l,r]$ 的最小代价，枚举最后合并位置 $k$：`dp[l][r] = min(dp[l][k] + dp[k+1][r] + sum(l,r))`。按区间长度递增计算。复杂度 $O(n^3)$。
''',
        },
        'graph': {
            'title': '图论基础',
            'summary': '存图、最短路、并查集、拓扑、生成树',
            'content': r'''
## 存图

**邻接表**（推荐，稀疏图）：

```cpp
vector<vector<int>> adj(n);
adj[u].push_back(v);  // 有向边 u->v
// 无向：adj[v].push_back(u);
```

带权边可存 `pair<int,int>`（邻接点, 权值）。

**链式前向星**：`head[]`、`to[]`、`nxt[]`，边多时常数更小。

## 并查集 Union-Find

维护连通性、Kruskal 最小生成树、离线询问：

```cpp
vector<int> fa(n);
iota(fa.begin(), fa.end(), 0);
int find(int x) { return fa[x] == x ? x : fa[x] = find(fa[x]); }
void unite(int x, int y) {
    x = find(x); y = find(y);
    if (x != y) fa[x] = y;  // 或按 size 合并
}
```

路径压缩 + 按秩 / 按大小合并，均摊接近 $O(1)$。

## 最短路

| 算法 | 复杂度 | 适用 |
|------|--------|------|
| BFS | $O(V+E)$ | 边权为 1 |
| Dijkstra | $O((V+E)\log V)$ | 非负权，单源 |
| Bellman-Ford | $O(VE)$ | 可有负权、判负环 |
| SPFA | 均摊较快，最坏 $O(VE)$ | 负权，易被卡 |
| Floyd | $O(V^3)$ | 全源，$V \le 500$ |

**Dijkstra**（堆优化）：

```cpp
priority_queue<pair<ll,int>, vector<...>, greater<...>> pq;
dist[s] = 0; pq.push({0, s});
while (!pq.empty()) {
    auto [d, u] = pq.top(); pq.pop();
    if (d != dist[u]) continue;
    for (auto [v, w] : adj[u])
        if (dist[u] + w < dist[v]) {
            dist[v] = dist[u] + w;
            pq.push({dist[v], v});
        }
}
```

**负环**：Bellman-Ford 松弛 $n$ 轮后仍能松弛则存在负环。

## 拓扑排序

**DAG** 上按依赖顺序排列，用于课程表、DP 顺序、判环。

**Kahn**：入度为 0 入队，弹出时减邻点入度；若出队数 $< n$ 则有环。

**DFS**：`vis` 三色（0 未访问 / 1 访问中 / 2 完成），后序逆序即为拓扑序；遇到 1 说明有环。

## 最小生成树 MST

**Kruskal**：边按权排序，用并查集依次加边，不形成环。复杂度 $O(E \log E)$。

**Prim**：类似 Dijkstra，维护到当前生成树的最小边权，稠密图可用 $O(V^2)$ 朴素版。

## 欧拉路径 / 回路

无向图：所有点度数为偶数（欧拉回路）或恰有两个奇度点（欧拉路径，从奇度点出发）。有向图看入度出度。DFS 删边 Hierholzer 算法构造。

## 强连通分量 SCC

有向图缩点：Tarjan 或 Kosaraju，$O(V+E)$。用于 2-SAT、DAG 上 DP 的缩点层。

## 例题

**例 1：最短路（堆优化 Dijkstra）**  
$n$ 个点 $m$ 条有向边，边权非负，求 $1$ 号点到各点最短路。邻接表 + 小根堆，弹出时若 `d != dist[u]` 则跳过过期状态。复杂度 $O((n+m)\log n)$。

**例 2：课程表 / 拓扑排序**  
$n$ 门课、先修关系 $m$ 条边 $u \to v$（学完 $u$ 才能学 $v$）。Kahn 入度为 0 入队；若最终出队数 $< n$ 则存在环，无法完成所有课。否则出队顺序即一种合法选课顺序。
''',
        },
        'datastructures': {
            'title': '数据结构',
            'summary': '栈、队列、堆、线段树、树状数组',
            'content': r'''
## 栈与队列

- **单调栈**：维护单调递增 / 递减下标，求「下一个更大元素」、柱状图最大矩形
- **单调队列**：双端队列维护窗口最值，滑动窗口、**DP 单调队列优化**（转移窗口有滑动界时）

## 堆（优先队列）

`priority_queue` 动态维护最值：Dijkstra、哈夫曼合并、多路归并、对顶堆维护中位数。

```cpp
priority_queue<int> max_heap;
priority_queue<int, vector<int>, greater<int>> min_heap;
```

## 树状数组（Fenwick）

支持**单点修改 + 前缀和**（或前缀最值等可逆运算），$O(\log n)$。

```cpp
void add(int i, int v) { for (; i <= n; i += i & -i) bit[i] += v; }
int sum(int i) { int s = 0; for (; i; i -= i & -i) s += bit[i]; return s; }
// 区间 [l,r] 和：sum(r) - sum(l-1)
```

二维树状数组：矩阵单点改 + 子矩形和。

## 线段树

**区间**修改 / 查询（和、最值、gcd、按位或等），建树 $O(n)$，单次 $O(\log n)$。

- **懒标记**：区间加、区间赋值时，延迟下传标记到子节点
- **动态开点**：值域很大、使用点少时，指针式建树
- **权值线段树 / 主席树**：可持久化，求区间第 $k$ 小

典型递归：`build`、`pushup`、`pushdown`、`query(l,r)`、`update(l,r,val)`。

## ST 表

**静态**区间最值（或 gcd 等满足重叠性质的操作），查询 $O(1)$，预处理 $O(n \log n)$，**不支持修改**。

`st[i][j]` 表示从 $i$ 开始长度为 $2^j$ 的区间最值，合并：`st[i][j] = max(st[i][j-1], st[i+(1<<(j-1))][j-1])`。

## 稀疏表 + 倍增

LCA：欧拉序 + `dep[]`，`fa[i][k]` 表示节点 $i$ 向上 $2^k$ 步的祖先，查询 $O(\log n)$。

## 字典树 Trie

每个节点存 26（或 2）个子指针，用于字符串集合、前缀统计、**01-Trie** 求最大异或和。

## 并查集 / 哈希表

并查集见「图论」；`unordered_map` 离散映射、计数时注意常数与 rehash。

## 选择建议

| 需求 | 首选 |
|------|------|
| 单点改 + 区间和 | 树状数组 |
| 区间改 + 区间查询 | 线段树 + 懒标记 |
| 只查静态区间最值 | ST 表 |
| 动态第 k 小 | 权值线段树 / 树状数组套平衡树 |

## 例题

**例 1：柱状图中最大的矩形（单调栈）**  
给定各柱高度 $h_i$，求最大矩形面积。维护**单调递增栈**存下标，当前柱比栈顶矮时弹出，以弹出柱为高、宽度为「当前 $i$ 到栈顶下一位」计算面积并更新答案。每个下标入栈出栈各一次，$O(n)$。

**例 2：区间修改区间求和（线段树 + 懒标记）**  
支持：区间 $[l,r]$ 加 $v$；查询区间 $[l,r]$ 和。线段树节点存区间和与「待下传加法标记」，`pushdown` 时把标记传给子节点。单次操作 $O(\log n)$。
''',
        },
        'math': {
            'title': '数学与数论',
            'summary': 'gcd、素数、快速幂、组合数',
            'content': r'''
## 最大公约数

`__gcd(a, b)`（C++17）或辗转相除法。

扩展欧几里得：求 $ax + by = \gcd(a,b)$ 的一组整数解。

## 素数

- **试除**：$O(\sqrt{n})$ 判素、分解
- **埃氏筛**：$O(n \log \log n)$ 筛 $1\sim n$
- **线性筛**：每个合数只被最小质因子筛掉

## 快速幂

$$a^b \bmod p$$ 在 $O(\log b)$ 内完成，注意取模每一步。

```cpp
long long powmod(long long a, long long b, long long mod) {
    long long r = 1;
    for (; b; b >>= 1, a = a * a % mod)
        if (b & 1) r = r * a % mod;
    return r;
}
```

## 组合数

预处理阶乘与逆元：

$$C_n^k = \frac{n!}{k!(n-k)!} \pmod p$$

$p$ 为质数时用逆元 `inv[k] = powmod(fac[k], p-2, p)`。

卢卡斯定理：大组合数模质数 $p$ 时，按 $n,k$ 的 $p$ 进制分解计算。

## 同余与逆元

- 费马小定理：$p$ 质数且 $p \nmid a$ 时 $a^{p-1} \equiv 1 \pmod p$，故 $a^{-1} \equiv a^{p-2}$
- 扩展欧几里得：求 $a^{-1} \bmod m$（$gcd(a,m)=1$）

## 欧拉函数

$\varphi(n)$：小于 $n$ 且与 $n$ 互质的个数。筛法预处理，分解质因数单次 $O(\sqrt n)$。

## 矩阵与线性递推

斐波那契第 $n$ 项：构造 $2\times 2$ 转移矩阵，快速幂 $O(\log n)$。

## 容斥原理

计数「至少满足一个条件」用全集减去不满足；多个条件用奇加偶减的 inclusion-exclusion。

## 例题

**例 1：快速幂求余（洛谷 P1226）**  
求 $a^b \bmod p$（$p$ 可能为合数时注意是否只用欧拉定理；质数模用快速幂即可）。二进制拆 $b$，平方累乘，$O(\log b)$。

**例 2：组合数取模（预处理阶乘）**  
从 $n$ 个点中选 $k$ 个的方案数 $C_n^k \bmod 10^9+7$。预处理 `fac[i]`、`inv[i]`，单次查询 `fac[n]*inv[k]%p*inv[n-k]%p`。$n$ 次询问总复杂度 $O(n + q)$。
''',
        },
    },
}
